# PDF and UI visual evidence

New integrated run: seven PDFs / eight pages fetched through the authenticated operator document API, then rendered with Poppler at 90 DPI. All eight pages were inspected in `document-contact-sheet.png`; the two-trip quote and the receipt total page were additionally inspected at full rendered size. `pdf-checks.json` verifies extractable text and explicit demo/no-real-transaction notices on every page. Exact PDF and rendered-image hashes are in candidate-manifest.json.

Observed: Isluno identity, guest/date/item labels, the two distinct trip dates, itemized USD 250.00 per trip, USD 500.00 multi-trip total, explicit sample provenance, simulated payment notices and per-ticket identifiers are visible. No clipped prices or missing footer demo notices were observed. The combined receipt uses two pages, with its aggregate total on page 2; the additional whitespace is a layout characteristic, not a missing item or total. This is English synthetic-flow visual evidence, not native-language approval.

Carried six-language PDF samples: `output/pdf/isluno-07/manifest.json` and `output/pdf/isluno-08/manifest.json` contain quote/receipt/ticket artifacts covering en/nl/de/es/pt/pap and six-item long-content examples. Their recorded hashes were rechecked against the unchanged files. Prior visual review and language-contract test evidence carry forward; no new native-speaker review is claimed.

Carried actual-shell/API screenshots: dashboard `output/isluno-09` (catalog management), `isluno-10` (journey/guest/quote/payment/delivery history), `isluno-11` (branding, controls, unknown/error/capability/other-tenant paths), and `isluno-12` (recovery plus saved-progress navigation). Dashboard source remains `2c99c3a13e01af625432e5f681d4c065e95dd1a8`. The recorded screenshot hashes were rechecked; no new browser execution is claimed. `carried-hash-checks.json` records 63 matching PDF/screenshot artifacts across these manifests. These are synthetic-authenticated local desktop/mobile flows, not outer-login or live-provider evidence.
