# Consolidated communication and operator-visibility rollout

Candidate backend ab88343b325d3ee67a6dcdad5e15e1f6dd2d254f; frontend25dfebfb8688f76ef357b97c60c893fdf1bb8791. Packet 6f0a71adb17f5fd40ff9a55ed79d41188078ce8aeb339754cdd541fa14130bfe. Not dispatched. Builder is sole host executor; Gatekeeper independently reviews this exact packet and performs authenticated post-swap Nr2 verification.

## Evidence and authority

Calvin explicitly requested the complete communication analysis, correction and deployment after the second no-response report. Existing conditional release authority is retained; independent technical clearance and session checks remain required. The original calvin-isluno-staging-c approval was consumed previously and is not replayed. Its partial archive, recovery files, manifests and receipts remain untouched.

Backend175 offline tests pass at parent e8aead27; the final preparation-question delta has46 affected communication/hospitality passes,10 presentation passes and1 additional binding guard pass. One initial command included a nonexistent test_presentation module; its loader error was corrected by the10-test test_presentation_tolerance run. No failing assertion remains. Root independently verifies71 tests,54 wire cases,5 corrected reproductions,10 frontend tests and the exact83-member archive. Frontend typecheck/build pass; all66 built frontend files match root's independent offline build. Full scripted hospitality export contains24 conversations71turns plus one nondispatched adversarial case; supplementary changed-branch/stale/error cases are separately labelled offline.

## Exact bounded action

One900-second cumulative allowance: one60-second serial upload of the2,826,240-byte archive (SHAd5c383222a336bba68af31105179af16b8e9f6a1c1405e306851fdec7ff62b5a), one120-second no-network build from exact current imagef3cac23e. No automatic retries, provider probes, customer messages, email, account/profile/config/model change, package installation, cleanup or database restoration.

Create only new private stage /root/isluno-communication-ab88343-a, with83 manifest-verified regular files (16Python,66frontend,1Dockerfile). Existing paths cannot be overwritten or reused. Prepare new public release /var/www/unboks-dashboard/releases/isluno-25dfebf-20260909-b. Verify every file hash before any activation.

Verify current runtime/container/client/profile/ICP/account/mounts/environment/producer sources and existing dashboard target. Keep tenant/config locks and producer fences. Require500seconds before disruption and300before activation. Stop producers; close ICP9→10 and runtime10→11; drain and seal only the exact reviewed history. Hash all customer/business tables before stopping, after the clean stop, after activation and after candidate import/readiness checks. No existing customer row is changed or relabelled.

Switch the runtime to the new image, verify all source hashes and actual entrypoint imports, exercise only a pure local wire renderer, check health/capabilities/catalog31, then atomically switch the dashboard pointer. Verify public HTTPS index, entry JS/CSS and Isluno operations chunk against the same manifest. Any server-side failure after activation permits at most one paired image/pointer rollback, preserving all data. Unknown state stays held.

Reopen runtime11→12 and ICP10→11; restore producers. The last180seconds of the cumulative budget are reserved for immediate authenticated Nr2 verification and, if necessary, one frontend-only pointer rollback. Gatekeeper uses its existing signed-in tab7 at https://dashboard.unboks.org/today, reloads it, verifies selected Isluno tenant, unchanged counts and new linked failure/part/reason presentation, and records DOM/screenshot evidence. It inspects link hrefs without opening customer conversations before the final protected-table check. Builder's browser session is separate and redirects to login; no credentials or tokens are extracted.

If authenticated UI verification fails or cannot complete within the reserve, ui_rollback.py may restore only the exact prior dashboard pointer within the original deadline. It checks the exact packet and rollback-payload hash and uses local/remote one-shot guards. The independently verified WhatsApp correction stays active under the existing backend-only readiness scope; no UI success is claimed. No second backend or frontend rollback is permitted after the server already rolled back.

## Preserved history and controls

Exact proof c20ab63aa42f4b705033ac0a59c1ee6b9334fa01d53c9d805499f844b5c9f6cc binds the prior nine-table hash6f5c245490da32498b684357fb9d444cffe0f34989979aa45a45b532b08576d8 plus the two existing escalation notifications/dedup links and current controls. Six retained turns, three accepted plans, two rejected plans, three historic resumed labels,174 earlier no-replay tombstones, no booking/payment/reminder jobs. The existing delivery-failure notifications are pending soft escalations, not hard takeover. Current ai_muted=false, ai_mute_source='', blocked=false. Old resumed labels are preserved as historical data; the new UI/API does not represent them as verified delivery.

The first extended proof check correctly stopped because its predicate expected technical notifications. A bounded read-only diagnostic showed the actual soft escalations, which match the durable failure insertion code. That predicate and its fixture were corrected; all consumed read guards and diagnostic payloads remain retained. No service disruption or customer mutation occurred during those reads.

The canonical reviewed files are dispatch.py, rollout.py, helpers.py, preflight.py, stage.py, retained_history.py, ui_rollback.py, their generated payloads, manifests and tests. prepare.py was initial local scaffolding and is not an executable release authority.
