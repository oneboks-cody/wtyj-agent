# Progressive visual discovery

Approved scope: improve the current Isluno/Tracy WhatsApp presentation without changing number, provider/model, booking authority, data or dashboard contracts. Baseline live source ab88343; branch codex/isluno-whatsapp-demo, verified BensonOpas/wtyj-agent worktree.

The current multi-product branch unconditionally omits media and uses numbered Choose labels. Latest live plan metadata shows three products, photo=none, exploration stage, two accepted text/button parts and zero media attempts. This is renderer/structured selection behavior, not evidence of provider media rejection.

Implement one durable plan containing concise common prose and separate product image/button parts. Optional model-authored per-product cards use existing catalog bindings; legacy replies retain a source-summary fallback. One/two relevant products are requested in the same model call. Broad introductions select no products and ask one easy question; specific enquiries answer directly. Every transmitted part owns exact product, image and button metadata; aggregate acceptance and existing callback, stale/duplicate, pacing and consent guards remain. Ordinary images only in this path. More photos pages remain bounded. Product-level galleries must not be presented as proof of a particular location; absent location provenance suppresses location-specific media. Missing local media yields labelled text; rejection/ambiguity never triggers blind media or text retries.

Inspect public video references read-only; no video activation without verified asset/association/format/authority. Images do not wait for videos. Source prices/facts unchanged. Inline location explanation uses verified catalog facts; unknown location facts remain unconfirmed.

Acceptance: actual handler→renderer→sender→post_once with strict fake HTTP and network denial; warm intro, separate product cards, scoped actions, paging, no booking for interest/photos, missing media, provider rejection versus ambiguity, partial second card, callback before result, stale/foreign/duplicate controls, oversize and UTF-16 bounds, one final question, mixed questions. Independent Gatekeeper source/payload/rendered-conversation review before a new bounded data-preserving rollout. Previous deployment guards are consumed and will not be reused. No engineering customer sends or paid probes. Preserve all current rows in a fresh exact proof; rollback retains them.

Verified inline place context is added for seven relevant products through a narrow catalog overlay (LOCATION-CONTEXTS.json). Every old catalog field, price, rule and gallery is retained. Sources identify the island of Klein Curaçao, Shete Boka national park, Piskado/Cas Abao beaches and the Playa Canoa swim stop. Catalog revision changes intentionally; old immutable snapshots and existing rows remain preserved. Rollout must bind the current catalog before applying only these additions and restore only this file if the exact reviewed rollback is needed. No location-specific image provenance has been established.
