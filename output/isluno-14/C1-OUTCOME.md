# C1 one-run outcome — observed, authority consumed

The exact approved C1 runner executed once under reference calvin-c1-once from source 739fde5b3b683c416ce826039058e3c5d86db50e. It exited 0. See c1-execution.json for exact UTC timestamps, monotonic duration, runner/helper/payload hashes and the schema-validated public projection. Both historical H1–H7 approvals and this C1 approval are now consumed. No retry or further access is authorized.

Matching Mermaid slugs, strict allowlist mode and exactly one account were observed. Config byte count 43,998 and SHA-256 1e8db71ac4fbe60d0a2eba42b96e066c77dce78d42e06cc493156c3dab5796a6 match the retained September 8 metadata. Mermaid reservation demo and reminders are true. The selected Isluno demo, cutover-coordination, activation generation, native carousels and media/document base fields are missing. Missing states are literal config observations, not claims about fallback defaults or loaded worker behavior.

The exact account ID remains only in tmp/isluno-c1-calvin-c1-once/binding.json (0700 parent, 0600 file). The runner result was schema checked against that private record without printing the account. Public evidence contains only its fingerprint. current_strict_account_id remains null in public required-inputs.json; a private binding reference and fingerprint identify the evidence without disclosure.

Remote observed_at is slightly ahead of the local subprocess completion timestamp; these are independent host clocks and are not assumed synchronized. No clock query was made. This does not replace fresh protected snapshots at any later config mutation.

No provider calls, live sends, DB/log reads, additional host discovery, configuration change, image operation, service operation or release occurred. Provider ownership/sole consumer (P1), loaded route binding (R1), actual admission/drain/bootstrap/retention evidence (I1/I2), approved media/document URLs, immutable image inputs, consistent backup/rollback and explicit release/canary authority remain separate gates. This observation proves configured intent only. Runtime and source package remain unchanged.
