Current C1 preparation: see **C1-APPROVAL.md** and **c1-evidence.json** for the offline runner candidate. No C1 execution is authorized or performed. Historical H1–H7 wording below describes its own scope; both attempts are consumed.

# ISL-14 proposed current-state inspection

**Historical inspection specification; both authorized attempts are complete.** The first attempt stopped atH4; the separately approved corrected attempt succeeded on2026-09-09. See `inspection-attempt.json` and `inspection-repeat-summary.json`. No further host access is authorized. The command forms and runner-review sequence below document those bounded inspections; protected/operator/provider projections remain outside their scope. Current release state is summarized in ACTION.md.

## Target and limits

Proposed host: `108.61.192.52`; expected tenant `mermaid`; expected container `wtyj-mermaid`; intended number `+1 223 276 0075`; frontend `dashboard.unboks.org`. These identities come from `target-discovery.json`, not a new observation. Use an existing authorized operator access path after host identity is verified; do not disable SSH host-key verification, discover credentials, read key files or reconnect the number. A host-key mismatch stops access.

One serial host inspection session, at most 120 seconds total, each check at most 15 seconds, no automatic retry or alternate credentials. Stop at missing permission, identity mismatch, unexpected output shape or limit. Return one sanitized JSON result, maximum 64 KiB, with UTC time, inspection packet commit/hash, per-check status and only the selected fields below. Omitted checks are `not_verified`, never implicitly passed. Capture no raw command output on parser failures; return the check ID and exit/status only. Do not upload raw metadata to GitHub. The reviewed runner candidate `wtyj/scripts/inspect_isluno_target.py` implements these limits; its execution still requires separate current-state access authorization. This document is not itself a runner.

No live model, messages, email, customer conversation, provider API, database query, Nginx reload, config write, image pull/build, restart, backup creation or deployment belongs to this inspection. External API test budget remains $0. Docker metadata reads and an explicitly authorized stdlib-only hash process are distinct from restarting application workers.

## Host checks and permitted output

| ID | Exact proposed check | Selected output and decision |
|---|---|---|
| H1 | `uname -s` and `uname -m` | OS and architecture only; bind OCI platform or stop if unsupported. No hostname, environment or process dump. |
| H2 | `docker inspect --format '{{.Id}}\|{{.Name}}\|{{.Image}}\|{{.State.Running}}\|{{.State.StartedAt}}\|{{index .Config.Labels "com.docker.compose.service"}}' wtyj-mermaid` | Container ID, exact name, immutable image ID, running boolean, start timestamp, Compose service label. Require name `/wtyj-mermaid` and expected service `agent`; missing label is unresolved, not a default. Do not inspect Config.Env, Cmd or arbitrary labels. |
| H3 | `docker image inspect --format '{{.Id}}\|{{.Os}}\|{{.Architecture}}' <image-ID-from-H2>` | Image ID/OS/architecture only. Validate the input as `sha256:` plus 64 hex digits. Compare image with September 8 baseline; a difference requires source/delta review before release. No image history or pull. |
| H4 | `docker inspect --format '{{range .Mounts}}{{.Type}}\|{{.Source}}\|{{.Destination}}\|{{.RW}}{{println}}{{end}}' <validated-H2-container-ID>` through a bounded field filter | Require exactly one writable bind mount for each documented pair: `/root/clients/mermaid/config` → `/app/config`, `/root/clients/mermaid/data` → `/app/data`, and `/root/clients/mermaid/logs` → `/app/logs` (see `clients/mermaid/docker-compose.yml:16–18`). Emit type, source path, destination and writable boolean only for the validated config/data pairs. For the validated logs pair emit only `expected_logs_mount_verified:true`; suppress its paths and never open or read logs. Missing or duplicate destinations, unexpected sources/destinations/types, non-writable mounts or malformed metadata produce only `mount_validation_failed:true` and stop for review, with no raw paths emitted. Validate the complete mount set before emitting any successful projection. No volume contents. |
| H5 | Stat only `/root/clients/mermaid/docker-compose.yml`, the H4 config directory, and H4 data directory plus `/state_registry.db` | Path, file type, size, numeric owner/group and permission bits; no Compose rendering/content and no DB opening/query. Symlinks or paths escaping the expected tenant root require review. Compose service ownership rests on H2 plus this path evidence; a label does not prove the file's current content. |
| H6 | `readlink -f /var/www/unboks-dashboard/current`; stat the pointer; hash only `index.html`, `assets/index-LhrQCX2T.js`, `assets/index-C6GIBlmS.css` under the resolved release | Pointer type/target and three file SHA-256 values or missing status. Require target under `/var/www/unboks-dashboard/releases/`; reject escape/symlinked files. Compare with the September 8 baseline. Different assets/release require scoped follow-up; do not crawl the webroot. |
| H7 | Stdlib-only hash process for the exact 112 relative `.py` paths in `isluno_baseline_manifest.json` | After confirming application root `/app`, hash regular files in place without imports/bytecode writes. Limit each file to 8 MiB and cumulative reads to 64 MiB. Emit relative path, digest or missing/nonregular/oversize status; compare with the baseline. No recursive walk or application import. This proves selected Python source identity, not dependencies or absence of extra runtime code. Requires explicit authorization for this read-only process in the container. |

Commands in the table are proposed forms, not a paste-and-run script. The backslash before a pipe is Markdown table escaping; the actual Docker format uses a literal pipe inside its quoted format. H4 raw metadata must remain in bounded process memory and pass the whitelist before any output. If a safe filter cannot enforce that, omit H4 and use an operator-supplied sanitized projection.

## Protected facts: operator-produced projections

Do not `cat client.json`, run `docker compose config`, dump Docker JSON or run `nginx -T`. These can expose credentials or unrelated tenants. The present audit authorization does not permit reading protected config. The following fields require either an existing authorized operator to produce a sanitized projection or distinct Calvin authorization for a reviewed, bounded local projection helper. No helper or protected read is executed by this packet.

**C1 — config:** Read exactly the verified tenant's `config/client.json` into memory once, maximum 1 MiB, using the existing canonical config lock/snapshot procedure. Hash those same bytes; parse with the standard library only. Emit SHA-256, byte count, `slug`, `business.slug`, `channel_account_allowlist.mode`, count of `channel_account_allowlist.zernio_accounts`, and a stable account-ID fingerprint for later equality checking. For the exact account ID, use a private operator binding record rather than a public issue. Emit only boolean/missing/invalid states for `features.mermaid_reservation_demo`, `features.mermaid_reminders`, `features.isluno_itinerary_demo_v1`, `isluno.native_carousels`, and whether media/document base values exist. Never emit arbitrary feature maps, account secrets, URL query strings, credentials, prompts or other config values. Require matching Mermaid slugs, strict mode and exactly one account. A local allowlist proves configured intent, not provider ownership. The hash is an observation; recompute under protection at any later write, never reuse a stale observation as CAS authority.

**R1 — route:** Operator projects only the currently loaded Mermaid dashboard API and inbound webhook routing: public host and path without query string, upstream host/port, tenant header value, matched location type, config provenance/hash and observation time. Require a single selected route to Mermaid `127.0.0.1:8102`, no fallback to another tenant and no second selected consumer. Candidate source expects `/api/mermaid/`; verify that exact public prefix instead of assuming it. A config-file projection alone is not proof that workers loaded it; identify the loaded revision or mark that distinction unresolved. Do not disclose certificates, credentials, raw Nginx files, other tenant routes or access logs.

## Provider-owned facts and ingress retention

**P1 — ownership/consumer:** Host evidence cannot establish current provider number ownership or exclusive delivery. An authorized account administrator must supply timestamped sanitized evidence from the existing provider console: intended number, business/account identity via private binding or matching fingerprint, active connection, selected webhook destination without tokens/query strings, subscription scope and number of active consumers. State the provider and evidence source. No provider API or console access is included here; no transfer, reconnect or display-name change is proposed. A historical successful send or one local container is insufficient proof.

**I1 — admission/drain:** Establish from the deployed source and operator configuration which component durably accepts inbound events, whether acknowledgement follows durable persistence, what survives a service stop, and how pending/in-flight work is drained. Record exact component/version, storage type/path metadata, supported admission command/mechanism, bounded drain condition and status-only observation surface. Inspect code/config evidence, not customer rows or logs. Distinguish in-memory queues from durable storage. No queue mutation, drain execution, service stop or synthetic callback is included.

**I2 — upstream retries:** Cite provider-owned documented retry/acknowledgement behavior and the actual subscription configuration. Record retention deadline, retry limits and how duplicate events are deduplicated by the deployed runtime. Documentation establishes a contract, not measured outage recovery. If durable retention or safe admission is absent/unproven, cutover stays blocked and a separately scoped implementation/verification proposal is required. Do not invent a maintenance switch or rely on a blind stop/recreate.

## Decisions after the inspection

1. Compare current metadata/source with the September 8 baseline; review intervening changes instead of overwriting them. Confirm the exact target, private account binding and single consumer.
2. Choose and pin a compatible immutable OCI base/platform/dependency recipe and final candidate image. Existing image identity alone is not base approval or dependency verification.
3. Bind approved HTTPS media/document bases to the actual Mermaid routes. Do not infer these from dashboard domain ownership. Public endpoint checks and provider fetching, if needed, require their own scope; none is included here.
4. Prepare the exact protected config diff, private consistent-backup destination/verification, retained-data rollback and tested admission/drain plan. No DB read/backup is performed by this inspection.
5. Present the final release digest/host/service/config/database/static pointer/window/rollback action for Calvin's explicit approval. Any live canary remains separately authorized and bounded.

The next permission is limited to a reviewed host metadata runner for H1–H7 and/or named operator projections C1/R1/P1/I1/I2. It is not permission to deploy or access providers. Obtain runner review before requesting authorization to execute it; do not ask Calvin to reconstruct inventory already recovered from the project.


## Implemented runner candidate and offline verification

`wtyj/scripts/inspect_isluno_target.py` implements H1–H7 only. Running it without flags prints `offline_default` and contacts no host. The proposed future command is `python3 wtyj/scripts/inspect_isluno_target.py --execute-authorized-host --authorization-reference <approved-record-id>` from this verified isolated checkout. The flag/reference records operator intent; it cannot grant authority. Do not run that command until Calvin authorizes the exact reviewed runner digest, host and H1–H7 scope. C1/R1/P1/I1/I2 are not implemented or collected.

The SSH command retains strict host-key checking, batch mode, one connection attempt, a ten-second connection timeout and bounded keepalives. No credential file is read by this runner; SSH uses the operator's existing authorized access configuration. Its collector runs stdlib-only Python, then one isolated hash-only process in the selected container; it never imports the application. The remote payload is generated from the exact local runner and the baseline's 112 approved relative source paths. H5 uses no-follow stat metadata and never opens the database file. Filesystem traversal uses no-follow directory descriptors; H6 permits only one release pointer to a 40-hex release directory under the documented root. Missing, changed, nonregular or unsafe files stop the check rather than yielding a partial pass.

Timeouts cover child commands and filesystem steps. The parent SSH session is bounded to 120 seconds; each remote check has a 15-second alarm, and the container hash process has its own 15-second alarm. Child stdout and stderr share a 64-KiB pre-buffering cap. Only schema-validated selected JSON can be emitted. On failure, output contains a fixed check/status and no partial records, raw stdout/stderr, exception text or unexpected paths. The logs mount produces only its verified boolean. Source/static reads are bounded to 8 MiB per file and 64 MiB per phase; growth/change is rejected. These installed limits govern the runner, not provider billing or deployment authority.

`wtyj/tests/isluno/test_target_inspection.py` supplies synthetic command/filesystem fixtures. The harness denies sockets/DNS, permits only Python fixture subprocesses (SSH/Docker cannot dispatch), and injects socket denial into those children. Fifteen focused tests pass, including complete H1–H7 fixture flow, mount order and whitelist, wrong identity/image, default transport off, authority-reference preflight, generated payload/isolated hash worker, command/session timeouts, combined output cap, path/symlink escapes, per-file/cumulative read caps, stat-only DB-path behavior and raw-result suppression. See `runner-tests.txt` and `runner-evidence.json`. No accepted application test/build/archive checks were repeated and no host/provider connection occurred.


### Runner review corrections R-R1 / R-R2

Owned process-group cleanup now runs even when the direct child has already exited. An absent group is handled safely, and direct-child reaping is bounded using reserved cleanup time inside the command budget. The regression proves a forked descendant started, triggers timeout/output-limit/nonzero-exit failure, waits beyond its intended delayed write, and verifies that no later marker was written.

H4 mount inspection and H7 hash execution now use the validated full H2 container ID, never the mutable container name. Before returning successful H7 output, the runner inspects the intended name again and requires the same running ID, image, start timestamp, name and service. This final guard shares H7's existing alarm and overall deadline. Synthetic replacement, restart, stopped-container and changed-image cases all return a stopped H7 result without partial metadata. This remains a point-in-time inspection; changes after its final check require fresh verification before any later action.


### Runner review correction R-R3

The mount parser now accepts the template's last-row LF plus Docker's additional terminal LF, removing at most two terminal LF characters. It does not trim interior records or arbitrary whitespace. Extra terminal blank lines, interior/leading blank records, CR/other separators, duplicates and unexpected mounts remain rejected. The full H1–H7 fixture now includes Docker's actual two-LF terminal framing. Fifteen focused offline tests pass.

Docker's official `cli/command/inspect/inspector.go` (blob `1f154c088ffae815d805ba4cc6710480f23798e0`, inspected via GitHub read-only) writes the rendered buffer and then `WriteByte('\n')`: https://github.com/docker/cli/blob/master/cli/command/inspect/inspector.go . This establishes an offline compatibility defect, not the confirmed cause of the prior live H4 stop. At that correction checkpoint the original one-run authority was consumed. A subsequently authorized corrected attempt is recorded in inspection-repeat-summary.json; both approvals are now consumed.
