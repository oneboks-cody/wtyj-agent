# Remaining evidence acquisition — scope review candidate, offline only

Prepared after accepted C1 outcome 2d68b843f1c9ebbe2b24b082716f7133c732f0df. C1 already proves matching Mermaid slugs, strict single-account configured intent at its recorded timestamp; its exact binding is private. Do not ask for or reread it. Both H1–H7 permissions and C1 permission are consumed. This design authorizes no access and implements no expanded collector.

## Existing locators and their evidentiary limits

| Surface | Known locator and provenance | What remains unknown |
| --- | --- | --- |
| Host/tenant | 108.61.192.52; wtyj-mermaid; Compose service agent, /root/clients/mermaid/docker-compose.yml; prior H1–H7 metadata | Later changes and effective runtime/process-manager configuration are not attested by historical metadata |
| Selected Nginx candidates | /etc/nginx/sites-enabled/api-unboks and /etc/nginx/sites-enabled/unboks-dashboard; local .github/workflows/deploy-dashboard.yml lines99–100 | Actual symlink targets, include/override chain and loaded revision; workflow is source evidence, not loaded routing |
| Public route candidates | api.unboks.org with expected /api/mermaid/ prefix; dashboard.unboks.org; workflow lines288–321 and ensure_dashboard_nginx.py tenant-upstream mapping | Actual selected inbound public host/path, route precedence and complete rewrite chain |
| Expected backend | Mermaid upstream http://127.0.0.1:8102/; X-Unboks-Tenant=mermaid; ensure_dashboard_nginx.py | Loaded request selection, fallback/second consumer, effective header injection and backend identity |
| Inbound handlers | /webhooks/zernio and /webhooks/meta/whatsapp in matched e43d0d1 webhook source | Which is subscribed, public prefix before rewrite, and provider applicability; do not assume Zernio from a local account allowlist |
| Static pointer | /var/www/unboks-dashboard/current -> recorded releases/2e41f4a209fbafc602419408696a25fa9791dc34 | Public API/media/document routing does not follow from static identity |
| Durable tenant store | /root/clients/mermaid/data/state_registry.db, mounted data path; H1–H7 stat only | Effective store use, complete accepted/in-flight dispositions and consistent retained-state evidence; no contents have been inspected |
| Old worker source | /etc/supervisord.conf inside intended container; accepted source lists webhook-server, email-poller, hold-reaper, ali-quote-recovery | Effective launch definition and external/uninstrumented writers; four program names are a starting list, not complete producer coverage |
| Candidate coordinator | wtyj/shared/mermaid_maintenance.py and authenticated /isluno/maintenance route in wtyj/dashboard/isluno_api.py | Not installed/activated proof; do not call this route on the old runtime or infer a public URL before R1 |

GitHub discovery now identifies the control plane at unboks-org/unboks-internal-control-panel commit 1ea5f95827a34a4f79c076b559cba5483ad38c80 and its existing admin locator https://icp.unboks.org/admin/tenants/mermaid (Channels / WhatsApp Business). Zernio and historical dedicated Mermaid provisioning are documented in backend issue323 / PR324 and the later release record. These are recovered source/historical facts, not fresh subscription/ownership verification. Exact current provider subscription, control-plane deployment/store binding, Nginx selected resolved targets and complete old-work disposition surface remain unverified. Do not ask for known provider/admin/host locators again.

Actual control-plane ingress source is POST /internal/api/zernio/webhook-router, forwarding to http://wtyj-mermaid:8001/webhooks/zernio with a12-second timeout. It checks stored unique account ownership, tenant generation and strict allowlist under a shared lifecycle lease. Retryable ownership returns503 with Retry-After5; forwarding failure502 and upstream>=400 propagate failure, but nonready/stale ownership can return202. This is not a buffering guarantee. The public Mermaid dashboard API route is not assumed to be provider ingress.

Do not call GET /internal/api/tenants/mermaid/channels/whatsapp/status as a read-only check: it can invoke provider synchronization and repair/mutate state. The admin page may fetch knowledge/control data and background refreshes, so UI navigation also needs side-effect review before automation. The webhook owner resolver can contact Zernio and self-heal; it is not a read-only probe. No such calls or navigation occurred.

## Consolidated acquisition sequence

Request two cohesive evidence bundles after gatekeeper scope review. Prefer sanitized records from the already authorized host operator and provider administrator. If Calvin wants the agent to collect a bundle, first implement and offline-review the exact bounded helper/UI scope with resolved locators, then seek one execution approval for that artifact. This document is scope design only; no collection command is ready to run.

**Bundle A — host operator: selected route, loaded revision and bootstrap/work evidence (R1 + I1).** The host operator starts with the two known Nginx candidate paths and the intended tenant. Do not repeat H1–H7/C1. The operator supplies selected facts from their existing configuration/release records and selected service-manager view, not raw files or screenshots containing unrelated tenants.

For routing, project the two intended public host/path pairs (dashboard API and inbound), matched location type, each selected rewrite/prefix transformation, resolved upstream host/port/path, effective tenant header, and whether any selected fallback/duplicate/second consumer exists. Include observation time and selected configuration provenance/hash. A default route or regex/include that may override selection must be resolved explicitly; absent context means unresolved, not false. Never emit credentials, certificate paths/contents, arbitrary headers, query strings, request logs, other tenants or raw Nginx text.

A files-only projection is labelled FILE_CONFIG_ONLY. An existing operator reload/deployment receipt can bind a selected configuration revision to its loaded generation. Alternatively, a separately approved current authenticated request can prove the selected dashboard request reaches a runtime identifying Mermaid, without requiring a historical receipt. The matched e43 source implements GET /dashboard/api/client/profile, with bearer authentication and a tenant slug derived from runtime environment or loaded config. The candidate public path is https://api.unboks.org/api/mermaid/dashboard/api/client/profile. A successful scoped check is labelled CURRENT_DASHBOARD_TENANT_VERIFIED; it does not attest the full Nginx revision, inbound POST route, sole consumer, image identity or config-file digest. GET /health returns only status:ok and cannot identify tenant. No request is executed by this plan. No synthetic webhook POST or reload is proposed.

For worker/admission evidence, bind the intended tenant's effective supervisor launch revision and named program enablement/status to the existing deployment record. Project only four fixed role booleans/states plus a bounded count and an unknown-producers flag; no command lines, process environment or shared process enumeration. The operator must additionally account for scheduled Mermaid reminders/reconcile/email/payment work, in-process inbound debounce/recovery/transport workers, operator writes, manual scripts and extra consumers from their managed inventory. Known program counts cannot prove that list complete; unknown producers block bootstrap. Preserve the distinction between existing Mermaid reminders and staged disabled Isluno reminders.

For bootstrap, request an EXISTING external admission/retention mechanism's exact selected component/version, tenant routing binding, configured acceptance/acknowledgement rule, storage durability boundary, supported close/status/reopen mechanism and retention deadline. Ask the operator to state “none” if absent; do not invent a buffer or deploy one during evidence collection. A tenant-only retryable rejection boundary is only a possible later design and depends on Bundle B's actual retry eligibility. Inbox-disable can acknowledge/drop input and is unsuitable. The old source lacks a complete coordinated barrier, so the candidate coordinator cannot protect its own first installation.

For accepted/in-flight work, an authorized operator must provide a consistent, timestamped status-only disposition report from an EXISTING supported surface if one exists. Required categories are pending inbound, active processing/leases, unresolved or failed outbound/business outcome, paused/superseded/missing or unknown records, and operator-reviewed retained no-replay work. Bind report to tenant, source/schema revision and admission epoch if available. Do not expose payloads, customer/message/contact IDs, amounts, tokens or raw DB rows. A process count or a queue size is not a terminal disposition; a vanished worker is not completed work.

No supported full old-runtime aggregate surface is currently established. Therefore this plan does NOT propose opening the SQLite database, calling a guessed endpoint or running ad-hoc SQL. If no existing report is available, explicitly mark the gap. Designing a bounded consistent read-only aggregate helper is a separate implementation candidate after reviewing exact schema, snapshot/WAL semantics, completeness and per-category meaning. Such a helper still cannot prove live external outcomes or close an admission race. Old-worker ambiguity requires an authorized reconciliation/disposition action later, never an inferred PASS from empty counts.

**Bundle B — provider administrator: ownership, selected consumer and retry configuration (P1 + I2).** The administrator uses the existing account/connection settings and selected webhook subscription settings for the intended Mermaid number. Zernio and the existing ICP Mermaid admin locator are already identified by GitHub; they confirm the current selected provider subscription and a side-effect-safe evidence surface privately; no new account, phone number, transfer, reconnect, subscription edit or webhook test.

Return one timestamped projection: intended number (private binding if appropriate), business/account binding comparison to the accepted C1 fingerprint, connection state, active selected subscription, event/resource-group selection, selected destination host/path with query/token removed, and count/scope of active delivery consumers for that number. Compare the account privately and publish equality/fingerprint only; do not copy account IDs, credentials or signed URLs into an issue. Sanitized destination omission must not hide ambiguity: indicate token/query presence as a fixed boolean while retaining exact private binding with the operator. Confirm whether direct Meta delivery or any second integration exists; a single listed Zernio webhook cannot establish global exclusive delivery.

Return actual configured acknowledgement/retry policy, enabled/disabled resource groups and the operator's basis for remaining retention/retry eligibility during a proposed interruption. Existing recorded public Zernio documentation (ingress-evidence.json; docs.zernio.com/webhooks and docs.zernio.com/webhooks/get-webhook-logs) describes a contract only. Do not apply it to a Meta subscription or assume every pending event retains the full retry schedule. If remaining event-specific eligibility cannot be established without private delivery records, report it unresolved; this bundle excludes delivery logs, payloads and authenticated delivery-log API calls. A later separately scoped sanitized aggregate or supported external buffer may be necessary. Do not pause/disable a resource group to simulate retention.

## Proposed collection limits for scope review

For a later host helper, propose ONE connection to the known host, strict existing SSH identity/host-key controls, 30-second total budget, 15-second selected read phase, 8-KiB combined stdout/stderr and fixed schema. At most the two named Nginx candidates may be read, each <=256 KiB, <=512 KiB total. Permit only a reviewed single symlink resolution inside /etc/nginx with identity rechecks; stop on additional chains, missing paths, escape, unsupported syntax, unresolved include/variable/regex precedence or oversize. Do not recursively include or search directories. A need for another selected include file must be named by the operator and reviewed before expanding the exact file list. These bounds may yield FILE_CONFIG_ONLY and cannot manufacture loaded-state proof.

Operator-provided service/reload/disposition receipts are limited to the selected Mermaid revision/roles and fixed status fields; no new process/DB collector is part of that helper proposal. Any receipt locator must be exact before automation. Fixed enums: verified/unresolved/not_present, enabled/disabled/unknown, file_only/loaded_bound; counts must be integers0..2147483647. Bound labels to128 characters, host/path values to2048 characters after removing query/fragment/userinfo, and selected route records to two. Overflow, unknown data shapes or partial failures stop the affected bundle without raw errors. Private comparison records require0700/0600 storage and must be excluded from Git; public outputs contain no arbitrary maps.

For the provider bundle, propose one administrator-produced projection from the selected account and subscription settings, at most two views plus a same-account consumer summary if the provider separates it. No API testing or repeated pagination by the agent, no sends and $0 metered calls. If the selected views cannot establish sole-consumer scope, report unknown and identify the exact missing view privately for review. No agent browser/connector interaction is authorized by requesting this operator evidence. All evidence must declare observation time, actor/source and limitation; screenshots or exports must be sanitized before sharing.

## Closure conditions and separate gates

R1 closes only with evidence for the actual selected dashboard and inbound routes: a scoped current tenant response may establish the dashboard portion, while inbound forwarding and exclusive delivery require their own matching evidence; P1 only with administrator-owned connection/number/account/consumer evidence consistent with C1; I2 only with that provider/subscription's applicable retention and retry eligibility. I1 remains incomplete until admission and all old work/producers have a supported, consistent and causally meaningful disposition path. A receipt or proposed command is not execution of a drain. If the existing surfaces cannot establish safe first installation, return a concrete design gap for review; do not hide it in an inventory checklist.

After scope review, consolidate any authorized operator request into these two bundles. Do not ask Calvin to rediscover known host/paths/account intent. Do not issue generic inventory requests or fragment each known field into another approval. Once genuine locators are supplied, any agent-side collector must still be implemented, offline-tested and reviewed before its single access request.

Image/dependency provenance, approved media/document URLs, fresh protected-write CAS, consistent backup and retained-data rollback, release window, deployment and live-canary authority remain separate. No new source/runtime changes or repeated tests/builds/archives accompany this plan. Counts remain13/14 accepted,0/14 merged/verified,3 deferred.


## Recovered bootstrap and stored-connection surfaces

Control-plane source app/channel_connections.py defines tenant_channel_connections (table declaration around336), with unique provider account/profile ownership and generation checks. Configuration default NR3_DB_PATH=data/nr3.db and Compose ./data:/app/data identify the store convention, not its exact effective host binding. Do not invent a host SQLite path or copy/open that DB under prior authority. A later consistent stored-record projection can supply status, provider, selected tenant, account fingerprint equality and bounded conflict counts; stored status is still distinct from a fresh provider subscription/connection assertion. Any such DB read requires a reviewed schema/snapshot/query scope and separately approved access.

Watchdog code at the same ICP commit identifies /root/clients/mermaid/.maintenance, /var/lib/unboks-tracy-watchdog/status.json and status.lock, unboks-tracy-watchdog.service/.timer, and /usr/local/lib/unboks/tracy_watchdog.py. The service runs each minute and can start/restart wtyj-mermaid after failures. The marker suppresses watchdog work at two checks; an already in-flight action can pass the last check before marker creation. The main function holds status.lock exclusively through its check/action/state write, suggesting a later reviewed coordination point, not a verified operational fence. Do not execute watchdog, create marker, acquire locks or change service state as inspection. It reads credentials internally and may restart the container.

Shared lifecycle lease in connect.py spans forwarding through tenant acknowledgement, not completion of background work. An exclusive lifecycle lock can block authenticated control callbacks needed by in-flight requests (PR97 deadlock history); it is unsuitable as an improvised admission/drain barrier. Marker suppression plus zero forwarding requests cannot prove complete old-worker disposition. Actual deployment of PR97/98, restart-policy coordination, accepted tenant work and external producers remain bootstrap evidence requirements.

Historical image/backup locator: matched backend e43 source wtyj/briefs/mermaid_abandoned_reminders.md records the observed reminder image tag and /root/backups/tracy-reminders-a095665-20260908T133845Z. This is neither an approved immutable OCI manifest/dependency closure nor current consistent-backup/restore proof. No backup directory has been read.

## Concrete next inspection scope for review — no execution

Propose ONE current dashboard tenant check, using the operator's existing authenticated session or a narrowly bound existing bearer credential made available by the operator without extraction from config, environment, browser storage or another tenant. Exactly GET https://api.unboks.org/api/mermaid/dashboard/api/client/profile, no body or query, no redirects, no automatic refresh/login/retry, TLS verification enabled, ten-second total timeout and8-KiB response cap. Do not navigate the ICP page or call its status API. Normal serving/access-log side effects may occur; no customer/runtime-state or provider operation is intended by this endpoint source.

Validate HTTP200 and a bounded JSON object; retain only timestamp, exact requested host/path, status code and slug equality to mermaid. Suppress display names, arbitrary fields, cookies/tokens, raw errors and response bodies; unexpected schema/status/redirect/timeout stops with a fixed outcome. A401 is not permission to discover credentials. No request is authorized or implemented yet. This checks one live dashboard path only; it is not a substitute for provider subscription/current ICP binding or old-work drain evidence.

For the remaining ICP bundle, the recovered admin and source locators eliminate broad discovery. First inspect existing GitHub deployment/config records for the exact effective NR3 store mount and any existing safe selected-record view; then prepare the concrete bounded projector if those facts suffice. Never substitute the mutating status endpoint. Subscription evidence remains administrator-produced unless a side-effect-safe exact API/view and authorization are established. Host route-file/receipt inspection is optional complementary evidence where needed, not a mandatory historical-receipt ceremony for the dashboard check. No expanded collector is implemented by this revision.

GitHub provenance: https://github.com/unboks-org/unboks-internal-control-panel/blob/1ea5f95827a34a4f79c076b559cba5483ad38c80/app/routes/connect.py ; https://github.com/unboks-org/unboks-internal-control-panel/blob/1ea5f95827a34a4f79c076b559cba5483ad38c80/host/tracy_watchdog.py ; https://github.com/unboks-org/unboks-internal-control-panel/pull/97 ; https://github.com/unboks-org/unboks-internal-control-panel/pull/98 ; https://github.com/BensonOpas/wtyj-agent/issues/323 ; https://github.com/BensonOpas/wtyj-agent/pull/324 . Historical source and incident reports narrow the evidence scope; none authorizes operations or proves current deployment.
