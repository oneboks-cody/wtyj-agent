# Consumed preflight and bounded correction

The approved first preflight stopped after 0.088 seconds at `icp_compose_unresolved`. Mermaid baseline image/service/mounts, tenant slugs and strict account fingerprint passed. ICP's service was identified, but it uses the base Compose file plus `docker-compose.override.yml`; the runner only accepted one file. This is a preflight limitation, not evidence that ICP is misconfigured. No host mutations, gate changes, agent messages or provider calls occurred. Isluno is not deployed.

The original payload is preserved byte-for-byte in `preflight-failed-original.py.txt`. The private original receipt remains under `tmp/isluno-deploy-approved-a/`; the public sanitized report is `preflight-execution-report.json`. The first dispatch guard remains consumed.

The corrected source is `wtyj/scripts/preflight_isluno_release.py`. It defaults to no host reads. Its explicit invocation is `python3 -I -B - --execute-approved-preflight` with the reviewed source on stdin, through the existing fixed SSH host/options and a 120-second enclosing deadline. This invocation is proposed only; it has not been dispatched. The remote process still has its own 112-second alarm and per-command deadlines.

The correction accepts only the observed two Compose paths in order, with no duplicate, extra or relative path, and hashes each file separately. It validates exact Mermaid and ICP mounts including count and RW mode before retaining those values. The shared ICP database gets a no-follow shape check without loading its contents; only the selected account mapping is queried, limited to two rows. The existing ingress source must match the known original ICP router SHA.

Command capture now uses the previously reviewed bounded streaming implementation. Selected file holders are matched by device/inode and container aliases, then classified against the selected container IDs and known unit cgroups; unknown holders stop. File names that might embed customer identifiers are not emitted. Existing source also identifies `nr3-provision-worker.service` as a possible operator writer, so the correction includes its fixed unit state in the selected producer projection. Stopping any shared host worker later requires accounting for its in-flight action and the resulting shared operational impact; its existence is not quiescence proof.

The Nginx check resolves only configuration sections already returned by `nginx -T`, binds the selected TLS server and exact Mermaid location/upstream/tenant header, and verifies the selected dashboard root. Missing/ambiguous includes or unsupported routing stop instead of turning unrelated matching strings into a PASS. The nine focused offline tests include complete synthetic preflight success/failure paths, bad Compose/mount/routing cases, a container-alias holder fixture, output/deadline bounds and the default no-read behavior. They do not prove the uninspected host checks.

Unchanged inputs: approved ACTION/RELEASE-SEQUENCE, application runtime, frontend, both images, config assets and the six-helper archive. The final source hash and test hash are in `preflight-execution-report.json`. No application suite or accepted image was rebuilt/retested for this correction.

Next action: independent review of this exact correction, then one additional bounded preflight allowance. The conditional coordinated deployment approval is already recorded and should not be requested again. A passing preflight still requires every later retirement, selected writer/watchdog fence, private backup, old-work aggregate, seal, authenticated tenant readiness and conditional reopen prerequisite in the approved sequence. Nr2 currently has a login form but no retained authenticated reviewer session; this remains a readiness prerequisite, not an Isluno success claim. There is no automatic retry or authority to bypass authentication, alter credentials, force a gate open, or extend a seal.
