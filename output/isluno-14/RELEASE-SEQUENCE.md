# Direct sealed release sequence

This is the current conditional sequence governed by ACTION.md, not an execution record. The prior compatibility-runtime startup proposal is superseded.

1. Complete the single 120-second selected preflight in ACTION.md. Bind actual service/container/mount identities privately. Stop on mismatch; do not guess ICP deployment paths. Before any production writes, require the final packet's action-specific authorization.
2. Create the new private backup root (0700) and config backup subdirectory (0700), load only hash-verified local image exports, stage public config assets without overwriting client.json, and stage the accepted dashboard archive into a new immutable release directory. Keep the previous images/compose metadata/static pointer as private rollback evidence. No running service changes yet.
3. Quiesce selected ICP operator/config writers. Retire the old ICP instance before the gate is relied upon. Initialize the Mermaid gate store with the new image's packaged `python -m host.mermaid_ingress_control initialize --execute-approved-action` against the verified ICP data mount, then start the reviewed ICP image using that same store. Initial generation 0 is closed. Verify no old router instance can still forward. This replacement has shared ICP impact. Never initialize a live store over an existing gate or treat initial closed state as proof of old-forward completion.
4. Fence the watchdog using the installed marker/lock contract. Stop its timer and account for the in-flight service under its lock; verify both cannot restart Mermaid. Stop the selected Mermaid app and other selected scheduled/delivery/operator writers. Do not run the watchdog as a probe. Preserve the private evidence reference covering admission, recovery, scheduled, delivery, transport, operator writers and runtime inventory. No exclusive lifecycle lock may be held across a callback path that needs it.
5. Under stopped writers, save private config/compose/pointer metadata and a consistent database backup with integrity verification. Project fresh config and main/WAL bundle digests without printing protected values. Create no secret-bearing public artifact. Stage the reviewed six configuration leaves through `apply_isluno_release_config.py --execute-approved-write --phase stage --expected-sha256 <fresh-private-CAS> --service-stopped-verified`. Record its new config digest. The canonical config lock must be the same host inode seen by all approved writers.
6. Run the new Mermaid image with Python as entrypoint, no network, no app supervisor, the verified config/data mounts, and the operator helper mount at `/release-tools/wtyj/scripts`. Set `PYTHONPATH=/app` and the existing selected tenant environment needed for the config path and strict identity. Run `prepare_isluno_sealed_start.py --execute-approved-seal --expected-config-sha256 <stage-receipt> --expected-db-bundle-sha256 <fresh-private-bundle> --verified-quiescence-reference <private-action-reference>`. It writes a new detached `/release-backup/state_registry.sqlite3` (never overwrite), classifies old work, and only with zero blocking records initializes/closes/seals the coordinator. It has a 45-second overall limit, 15-second backup limit, and 120-second seal. An existing coordinator/cutover is a stop for this first-install tool.
7. While that exact seal remains valid, run the same pinned image with Python entrypoint and `apply_isluno_release_config.py --execute-approved-write --phase activate --expected-sha256 <stage-receipt> --sealed-generation <one-shot-generation> --service-stopped-verified`. It checks `require_sealed` against the selected database before writing. Then start only the selected Mermaid app with the reviewed image. Startup performs the accepted transition under the same seal; retain original records and quarantine. Do not rerun or extend an expired seal automatically.
8. Before admission reopens, verify selected tenant capabilities, catalog count, transition marker/generation and owner controls. Switch the shared Nr2 static pointer to the staged accepted release and verify loaded assets/routes. Authenticate with the existing selected account/session; no provider send is needed. Check public media/document handlers without disclosing signed customer documents. Record exact results. A login page or generic 200 does not prove Isluno readiness.
9. Reopen the exact runtime generation using the accepted coordinator only after readiness, then use packaged ICP `reopen --execute-approved-action --expected-generation <observed-gate-generation> --runtime-ready-verified`. Preserve the gate store and guard-capable images. Release only the temporary producer/watchdog fences appropriate to the reviewed resulting config; reminders remain disabled. Hand Calvin the verified dashboard link for manual testing.

Failure at any step stops dependent actions with admission closed. Preserve snapshots, CAS receipts, gate state and unresolved records; use only the guard-preserving rollback described in ACTION.md. Never call a historical deployment script with its previous-image rollback unchanged.
