CURRENT UPDATE: local image build and65 unique image tests are complete; see local-image.json, RELEASE-SEQUENCE.md and BOOTSTRAP-MINIMUM.md. Historical absent-image/C1-unread statements below describe their prior checkpoint. No deployment is authorized or performed.

# ISL14 current release packet — preparation accepted; execution still gated

Current accepted backend runtime: **e2deab8c2ad6c4313324d5b94769b91a1f0ae168**. Dashboard source: **2c99c3a13e01af625432e5f681d4c065e95dd1a8**, unchanged. Gatekeeper independently accepted the maintenance implementation and its focused regressions. This packaging update changes documentation/reproduction evidence only. Counts remain13/14 accepted in candidate,0/14 merged/verified,3 deferred; no ISL14 live criterion is implied complete.

## Current artifacts

Use `current-artifacts.json` and `current-package-checks.json` for this runtime. `artifacts.json` is the preserved historical manifest for backend4f62147, not the current source selection.

| Material | Identity | SHA-256 |
|---|---|---|
| Current backend source archive | `tmp/isluno-release-artifacts/e2deab8c2ad6c4313324d5b94769b91a1f0ae168/isluno-backend-source.tar.gz`;60,635,790bytes;365 files | `d9841ef3070fed9cde8840a353ad7a9917400f7facfbd13d3ab50472c9b68c51` |
| Unchanged default-off config archive | Original `tmp/isluno-release-artifacts/isluno-config-default-off.tar.gz`;39,521bytes;5 files. Git confirms identical files at accepted runtimee2deab8. | `d3765cd214b55501b5cf26ec152f8cd49fb733eba0feed09f6d9b40836fa3c63` |
| Unchanged dashboard static archive | Original `tmp/isluno-release-artifacts/isluno-frontend-static.tar.gz`;738,983bytes;66 files;accepted source2c99c3a. | `9faecd003e4d32da89bf032e755307b39e879e937414e753eb22b3f2090d4dd2` |
| Backend OCI image | **Absent.** No image built, pulled or published. Linux/AMD64 was observed, but immutable compatible base/dependencies and final image are not pinned. | No digest |

Paths above are relative to `/Users/calvin/Projects/isluno-whatsapp-demo-20260908`. Source membership, blob identities, content hashes and modes were compared with Git; all17 maintenance runtime files are included. No tests, protected configuration, databases, customer records, secrets, symlinks or submodules are packaged. The old archive bytes and metadata remain preserved. No frontend/config rebuild or unaffected artifact matrix was repeated. Source archive is not an OCI image.

`BUILD.md` gives the current pinned, local-only reproduction command. It refuses to overwrite the versioned archive. `package-local-artifacts.py`, `artifacts.json`, `local-validation.json` and the earlier static-build log are historical evidence only; do not use that earlier writer to reproduce the current backend.

## What has actually been verified

- ISL13's original journey evidence remains historical acceptance evidence. The revised runtime passed221 backend tests with sockets/DNS denied in27.329seconds;23 focused maintenance tests passed. Gatekeeper independently reran the23 focused tests and failure reproductions. See `maintenance-evidence.json`, `maintenance-tests.txt` and `maintenance-regression.txt`.
- The first separately approved H1–H7 attempt stopped atH4 with no usable retained bindings. The second separately approved corrected-runner attempt completed on2026-09-09 at02:05:54UTC. Linux/AMD64, expected mount metadata, baseline image/start identity,112 selected Python hashes and the frontend release/three asset hashes matched. Final container identity was stable. See `inspection-attempt.json` and `inspection-repeat-summary.json`. Raw selected metadata remains private.
- Ten point-in-time metadata fields are populated in `required-inputs.json`. Selected file matches do not attest all dependency binaries, complete source/build identity, effective protected settings or public/provider routing; source-SHA execution inputs remain null for that reason.
- The two host-access approvals are complete and consumed. No provider access, live send, protected config/DB-content/log/customer read, image build, merge, deploy, restart or activation occurred. Runtime code and gate tables have only been exercised against offline synthetic fixtures.

## Proposed tenant changes and preserved behavior

Technical tenant remains `mermaid`; customer brand/journey is Isluno / `isluno_itinerary_demo_v1`; intended existing number is +1 223 276 0075. Number ownership and exclusive routing are still unverified. No second consumer, reconnect, transfer or provider profile mutation is proposed.

The accepted runtime adds dedicated **inactive-by-default** Mermaid maintenance coordination. Admission and normalized-message/dedup acceptance share a transaction. The29 participating entry points track durable work; every closing-snapshot member requires an explicit reviewed completion/disposition. Missing/failed/unknown/superseded outcomes and ambiguous sends stop seal. Authenticated operator status exposes bounded metadata/counts. It does not make an empty count or an inventory declaration proof of readiness. See `MAINTENANCE.md` and `maintenance-coverage.json` for exact coverage and external exclusions.

A future authorized staging action installs the five reviewed Isluno config files with feature activation false. Any final protected config diff must explicitly bind the dedicated coordination flag, its sealed activation generation, Isluno feature activation, native_carousels=false and approved HTTPS media/document bases. Preserve slug, strict single-account binding, credentials, owner controls and unrelated settings. Reminders remain disabled. Historical config hashes are not fresh CAS values; no current merged protected diff exists yet.

The future backend action must select one verified Mermaid service/image and preserve mounted data/config and peers. The frontend action stages the unchanged static artifact and switches the verified release pointer only after compatible backend readiness; it does not deploy the TypeScript API server or rewrite shared Nginx routing. Database initialization remains additive and guard-preserving, with original rows/journals retained. Backups must remain private on the approved target.

## Required sequence and remaining gates

1. **First-install prerequisite:** the matched old runtime lacks the new gate. Establish a verified external admission/retention boundary and old-worker/in-flight disposition before any compatibility-runtime stop/install. No such external boundary or complete old-producer inventory is currently evidenced. Public retry documentation alone cannot establish this. Inbox-disable returns200 without retention and cannot substitute. The new gate cannot protect its own first installation.
2. Complete C1/R1/P1/I1/I2 operator/provider facts through separately scoped authorization: protected strict-account/feature projection and fresh digest; loaded public routing; actual number/subscription/sole consumer; ingress retention and complete producer/worker inventory. Neither successful H1–H7 metadata nor coverage declarations satisfy these facts.
3. Pin a compatible immutable base/dependency recipe and final backend OCI image containing the accepted current source. No image operation is authorized by this packet. Bind public media/document routes, private consistent-backup/restore verification, exact protected CAS diff, process inventory, owner controls and change window.
4. Review the staged compatibility/default-off rollout in `MAINTENANCE.md`. Any first installation, enabling coordination, drain/seal, service change, additive DB/cutover action, static switch and reopening must appear in Calvin's concrete action-specific release authorization. Current function/command descriptions are review material only.
5. During a separately approved final cutover, close admission under the verified mechanism, finish/dispose retained original work, seal the exact generation within the bounded deadline, apply the approved activation-generation/config diff, and verify the guard-capable runtime before reopening. Unknown/ambiguous work blocks promotion. No force-ready, expiry retirement or invented payment/booking completion.

`required-inputs.json` retains16 unresolved fields and points to their evidence/gaps; no generic inventory re-request is made. Current public URLs, native provider rendering/outer login/live controls and language sign-off are not invented passes. Both approved inspections have ended; this packet grants no new access.

## Rollback and authority

After activation, retain the guard-capable runtime, current database, cutover marker and all new ledgers. Restore only explicitly approved prior feature leaves under fresh CAS and, if compatible, the prior static pointer. Uncertain sends do not replay. Never replace current data with an old backup or switch to a pre-fence binary. The historical `deploy_mermaid_release.sh` previous-image rollback remains unsuitable unchanged.

This packet is prepared for independent packaging review and eventual concrete release authorization. The current source archive can be reviewed now; the remaining bootstrap/ownership/config/image/URL/backup/window facts prevent an executable release action from being approved yet.
